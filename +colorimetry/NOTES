# To Do

This whole section needs to be cleaned up and formalized a little more. Right now it is more of a collection of functions and scripts that use those functions. It should be a little library of colorimetry functions and then scripts that use them, which can be stored separately.

--